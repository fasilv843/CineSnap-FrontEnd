import { SeatStatusDirective } from './seat-status.directive';

describe('SeatStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new SeatStatusDirective();
    expect(directive).toBeTruthy();
  });
});
