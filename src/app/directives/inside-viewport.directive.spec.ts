import { InsideViewportDirective } from './inside-viewport.directive';

describe('InsideViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new InsideViewportDirective();
    expect(directive).toBeTruthy();
  });
});
